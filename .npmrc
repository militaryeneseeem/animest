# Keep local, CI, and Vercel installs on the exact pnpm version declared in package.json.
package-manager-strict=true
manage-package-manager-versions=true

# Keep pnpm's default isolated linker so Turborepo can build a stable package graph.
node-linker=isolated
